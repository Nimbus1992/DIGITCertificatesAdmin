Delete the entire "GENERATED EXPERIENCES" `<section>` block from `src/components/service-config/OverviewWorkspace.tsx` (the two cards for Citizen Portal and Employee Workspace). Also remove now-unused `User` and `Briefcase` lucide imports. No other files affected.